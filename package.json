{
  "name": "ui2-react",
  "workspaces": [
    "packages/*",
    "www"
  ],
  "scripts": {
    "build": "yarn workspace ui2-components build",
    "start": "yarn build && yarn workspace ui2-www start"
  }
}
